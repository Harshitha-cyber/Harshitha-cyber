def sum_of_squares(n):
    return sum(i ** 2 for i in range(1, n + 1))
n = 5
print("Sum of squares (loop method):", sum_of_squares(n))

def sum_of_squares_formula(n):
    return (n * (n + 1) * (2 * n + 1)) // 6

print("Sum of squares (formula method):", sum_of_squares_formula(n))
